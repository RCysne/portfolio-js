// Criando a função para criar o db, e converter em JSON o que será lido através do getItem
// const getTask = () => JSON.parse(localStorage.getItem('db_client' || '[]'))

// // Criando a função para setar no db o item, transformando o db em string
// const setTask = db_client =>
//   localStorage.setItem('db_client', JSON.stringify(db_client))

let class = {
  name: 'Ronaldo', 
  language: 'Javascript', 
  age: 46
};


  const classLocal = () => localStorage.setItem()
